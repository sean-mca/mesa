syntax = "proto3";

package register;

service Register {
    rpc Reg (RegisterRequest) returns (RegisterResponse);
}

message RegisterRequest {
    string id = 1;
    string timestamp = 2;
}

message RegisterResponse {
    string confirmation = 1;
}
